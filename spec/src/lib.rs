// git-std-spec: snapshot reference scenarios.
//
// This crate is test-only (`publish = false`). All test scenarios
// live in `tests/` and use either `trycmd` (stateless CLI output)
// or `snapbox` + `TestRepo` (repo-dependent scenarios).
